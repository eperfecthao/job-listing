<h2 class="text-center">新建简历</h2>

<%= simple_form_for [@job,@resume] do |f|  %>
  <%= f.input :content, label: "内容" %>

  <%= f.input :attachment %>

  <%= f.submit "提交", data: {disable_with: "Submiting..."} %>
<% end %>
